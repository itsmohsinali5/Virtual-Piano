# No API keys or secrets are required to run this project locally.
# Hand tracking and audio synthesis run entirely in the browser.
#
# Optional: copy this file to .env.local if you later add environment-specific
# configuration for your own deployment.
